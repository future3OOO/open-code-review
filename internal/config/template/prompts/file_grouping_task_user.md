Group the following changed files for code review. Each group's total changed lines should stay under {{grouping_line_threshold}}.

<changed_files>
{{file_list}}
</changed_files>

Return the grouping result as a JSON array.